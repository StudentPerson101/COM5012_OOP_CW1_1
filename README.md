The actual executable code is stored within the file name "p4 - code implementation - 2.txt".

It possesses no external packages, only what is shipped with standard C++17/G++13.3.0 compiler, so no installations need to be made beyond installing a sufficient C++ compiler.
